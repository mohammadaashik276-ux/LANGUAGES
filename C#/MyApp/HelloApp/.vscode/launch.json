{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run HelloApp",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "Run HelloApp",
            "program": "${workspaceFolder}/bin/Debug/net10.0/HelloApp.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopAtEntry": false
        }
    ]
}
